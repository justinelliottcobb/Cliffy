# Local environment files (contain host-specific config)
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Build output
dist/

# Vite cache
.vite/

# Lock files - not committed due to cross-platform CI (macOS/Windows/Linux)
# npm generates platform-specific optional deps that break on other platforms
package-lock.json
*/package-lock.json
