{
  "name": "{{projectName}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/main.ts",
    "build": "bun build src/main.ts --outdir dist --minify",
    "preview": "bun run dist/main.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cliffy/core": "^{{cliffyVersion}}"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.0"
  }
}
